import React from 'react';
import Menu from '../menu/Menu';
import "./Recent.css"

const Recent = () => {
    return (
        <div className="baseContainer">
            <Menu/>
        </div>
    );
};

export default Recent;